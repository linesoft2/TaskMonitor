<Application x:Class="task_monitor.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:task_monitor"
             xmlns:ui="http://schemas.inkore.net/lib/ui/wpf/modern"
             ShutdownMode="OnExplicitShutdown">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <!-- Core theming: palettes, brushes, light/dark/high-contrast schemes. -->
                <ui:ThemeResources />
                <!-- Modern Fluent styles for standard WPF controls (Button, TextBox, ...). -->
                <ui:XamlControlsResources />
                <!-- FluentWpfCore: persistent acrylic via WindowMaterial (DetailWindow) + its theme resources.
                     The taskbar context menu no longer uses its ContextMenu styles — it uses iNKORE's, above. -->
                <ResourceDictionary Source="pack://application:,,,/FluentWpfCore;component/Themes/Generic.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <!-- Subtle Fluent 2 chrome button for header icon toggles (iNKORE ships no
                 standalone icon toggle button): transparent until hover, 4px radius, theme
                 fills; IsChecked keeps the hover fill lit. Shared by DetailWindow's pin
                 toggle and NetDetailView's public-IP eye toggle so both have identical
                 hit area / hover look / arrow cursor. -->
            <Style x:Key="ChromeButtonStyle" TargetType="ButtonBase">
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
                <Setter Property="Width" Value="28" />
                <Setter Property="Height" Value="28" />
                <Setter Property="FocusVisualStyle" Value="{x:Null}" />
                <Setter Property="SnapsToDevicePixels" Value="True" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ButtonBase">
                            <Border Background="{TemplateBinding Background}"
                                    CornerRadius="4" SnapsToDevicePixels="True">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource SubtleFillColorSecondaryBrush}" />
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource SubtleFillColorTertiaryBrush}" />
                                    <Setter Property="Foreground" Value="{DynamicResource TextFillColorSecondaryBrush}" />
                                </Trigger>
                                <!-- Checked state: keep the hover fill lit. -->
                                <Trigger Property="ToggleButton.IsChecked" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource SubtleFillColorSecondaryBrush}" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
